apply plugin: 'com.android.library'

android {
    namespace 'com.android.colorpicker'
    compileSdk 35

    defaultConfig {
        minSdk 35
    }

    sourceSets.main {
        java.srcDirs = ['src']
        res.srcDirs = ['res']
    }
}
